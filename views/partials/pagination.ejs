<div class="pagination-container">
    <!-- Pagination Info -->
    <div class="pagination-info">
        <span id="paginationInfo">
            Menampilkan <span id="startItem">0</span>-<span id="endItem">0</span> dari <span id="totalItems">0</span> item
        </span>
    </div>

    <!-- Pagination Controls -->
    <div class="pagination-controls">
        <!-- Page Size Selector -->
        <div class="page-size-selector">
            <label for="pageSizeSelect">Tampilkan:</label>
            <select id="pageSizeSelect" class="form-select form-select-sm">
                <option value="10">10</option>
                <option value="25">25</option>
                <option value="50">50</option>
                <option value="100">100</option>
            </select>
        </div>

        <!-- Pagination Navigation -->
        <nav aria-label="Page navigation">
            <ul class="pagination" id="paginationNav">
                <!-- Pagination will be generated dynamically -->
            </ul>
        </nav>
    </div>
</div>

<script>
// Pagination functionality will be handled by the main JavaScript file
</script>